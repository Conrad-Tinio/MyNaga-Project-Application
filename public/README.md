# Public Assets

Place your logo files here:

- `logo.png` or `logo.svg` - Main logo for navbar (recommended: PNG with transparent background, or SVG)
- `favicon.ico` or `favicon.png` - Browser tab icon (recommended: 32x32 or 64x64 pixels)

The logo will be automatically available at `/logo.png` or `/logo.svg` in your application.

